# CryptoNinja Cube

咲耶のイラストを使った、ブラウザ向け3D絵合わせキューブの操作試作です。咲耶1面と方向・番号付きの仮タイル5面を使用しています。

## 起動

Node.js 22を推奨。

```sh
npm ci
npm run dev
```

http://127.0.0.1:5173/ を開きます。Windowsでは `start-local.cmd` でも起動できます。このPCのみ接続可能です。ポート使用中はエラーで停止します。停止は端末でCtrl+C。

## 遊び方

- 初期設定は「1面完成」。3手または12手混ぜて開始します。
- 全6面が未完成になるシャッフルだけを採用します。シャッフル中はクリアしません。
- どの面でも位置と向きが揃えばクリア。3手混ぜても1〜2手で揃えば即クリアです。
- 完成した面の9ピースだけが接合し、絵が正面を向きます。クリア後の回転・戻す操作は停止。「もう一度遊ぶ」で再挑戦できます。
- 「6面完成」は従来の操作試作。6面完成時に全体が接合します。
- PC：マスをクリックしてWASD。再選択まで軸・列は固定。W/S・A/Dは逆操作です。
- スマホ：200ms長押し後、縦横に24px以上スワイプ。直接押せる矢印もあります。
- 余白ドラッグで全体を眺めます。選択は金色、上下は赤、左右は緑。
- 表示サイズは小・中・大。ピースの隙間・低モーションも変更できます。
- 1面モードの「操作」はシャッフルを含まず、プレイ中の戻す操作は含みます。タイムアタックの公式スコアではありません。

## 開発・検証

```sh
npm test
npm run build
# 別端末でnpm run devを起動後（Edgeが必要）
node browser-check.mjs
node oneface-check.mjs
```

整数座標と基底でパズル状態を管理し、描画の補間回転とは分離しています。49項目の自動テストで回転・逆操作・開始配置・早期クリア・再挑戦を検証。ブラウザではPC操作とタッチ入力の模擬試験を実施。実スマホの操作感・性能は未確認です。

本番ビルドは成功しますが、大きなJSチャンクの警告が残ります。配信サイズ最適化は今後の課題です。window.__cubeは操作試作の読取診断用です。

## 素材と公開範囲

このリポジトリはゲームシステムのみを収録し、画像・動画は含みません。手元で利用許可のある画像を `public/assets/sakuya.png` に配置してください（フォルダがなければ作成）。画像なしでもキューブは仮タイルで描画されますが、画像読み込みの案内が表示され、採用イラスト閲覧は利用できません。

画像は面全体のUVで左右反転します。`public/assets/` と画像・動画ファイルはGit管理対象外です。ローカルの素材を誤ってPushしないでください。

ギャラリー保存・ランキング・タイムアタック・動画・効果音は未実装。採用イラスト閲覧ボタンは試作確認用で、報酬解放機能ではありません。ゲームのWeb公開設定は行っていません。
